# Plano — P0 Performance, mobile, SSE e fontes

1. Escrever regressões para compactação, persistência grande e detalhes sob demanda.
2. Implementar compactação de CSV e GZipMiddleware no FastAPI.
3. Consumir `/query?compact=1`, limitar persistência/broadcast síncronos e liberar `/stats` no proxy.
4. Restaurar observações via `/detalhes` na modal e no drawer.
5. Implementar shell responsivo com drawer móvel, backdrop e alvos de toque.
6. Remover Google Fonts, alinhar CSP e adicionar `/events` aos proxies Node/Vite.
7. Rodar testes focados, suíte completa, lint, build e QA real em quatro viewports.
